---
title: "Getting Started"
description: "Installation, prerequisites, and first steps with Relay"
order: 1
---

## Prerequisites

- [Node.js](https://nodejs.org/) >= 18 (or [Bun](https://bun.sh/))
- A Telegram bot token from [@BotFather](https://t.me/BotFather)
- Your Telegram user ID (get it from [@userinfobot](https://t.me/userinfobot))
- Credentials for at least one coding agent provider (see [Providers](/docs/providers))

## Installation

### From npm

```bash
npm install -g relay
```

### From source

```bash
git clone https://github.com/Harsh-2002/Relay.git
cd Relay
npm install
npm run build
```

## Configuration

Copy the example environment file and fill in your values:

```bash
cp .env.example .env
```

```env
BOT_TOKEN=your-telegram-bot-token
ALLOWED_USER_ID=your-telegram-user-id
PROVIDER=opencode
```

- `BOT_TOKEN` — from [@BotFather](https://t.me/BotFather)
- `ALLOWED_USER_ID` — your Telegram user ID
- `PROVIDER` — `opencode`, `claude`, or `codex`

Then configure your chosen provider. See [Providers](/docs/providers) for details.

## Run

```bash
relay
```

Or from source:

```bash
npm start
```

Open your bot in Telegram and send `/start` to verify the connection. Use `/help` to see all available commands.

## Next Steps

- [Configuration](/docs/configuration) — All environment variables
- [Providers](/docs/providers) — Provider setup
- [Commands](/docs/commands) — Full command reference
- [Features](/docs/features) — Streaming, voice, MCP, file attachments
- [Troubleshooting](/docs/troubleshooting) — Common issues
